# Copy to `.env.build.values` if your ScaleTrain setup requires an env values file.
# This image uses public PyPI/PyTorch indices and does not need CodeArtifact.
MOUNTABLE_AWS_FOLDER="${HOME}/.aws-mountable"
