# Out-of-tree NeMo-RL adapters for the gemma-4 E2B DAPO exact-replication run.
# Nothing in third_party/nemo-rl is modified; run_grpo_repro.py registers these
# classes (dataset via dotted path in the yaml, env via register_env()).
